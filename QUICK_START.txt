# ⚡ QUICK START GUIDE

## 🚀 Run Your Project in 5 Steps

### Step 1: Install Backend Dependencies
```powershell
cd backend
npm install
```

### Step 2: Start MongoDB (if local)
```powershell
mongod
```

### Step 3: Start Backend Server
```powershell
npm start
```
✅ Wait for: `Backend running on port 5000`

### Step 4: In New Terminal - Start Frontend
```powershell
cd frontend
npm install
npm run dev
```
✅ Wait for: `Local: http://localhost:5173/`

### Step 5: Open Browser
Open: **http://localhost:5173**

---

## 🎯 Default Credentials for Testing

**Test Account:**
- Email: `test@example.com`
- Password: `password123`

(Or register a new account)

---

## ✨ Features to Test

1. **Wishlist Count** ❤️
   - Add items to wishlist
   - Check navbar count updates instantly
   - Logout and login to verify persistence

2. **Profile Edit** 👤
   - Click "Profile" → "Edit Profile"
   - Update your details
   - Switch between Buyer/Seller role
   - Click "Save Changes"
   - See success toast

3. **Items Filter** 🔍
   - Go to "Items" page
   - Use filter panel on left
   - Search by name, category, location, price
   - Click "Apply Filters"
   - Items grid updates

4. **Shopping** 🛒
   - Add items to cart
   - See cart count update
   - Add items to wishlist
   - See wishlist count update

---

## 📊 Ports

| Service | URL | Port |
|---------|-----|------|
| Backend | http://localhost:5000 | 5000 |
| Frontend | http://localhost:5173 | 5173 |
| MongoDB | mongodb://localhost:27017 | 27017 |

---

## 🆘 Troubleshooting

**Backend won't start:**
```powershell
# Check if port 5000 is in use
netstat -ano | findstr :5000
# Kill process if needed
taskkill /PID <PID> /F
```

**MongoDB connection error:**
- Make sure `mongod` is running in another terminal
- Or use MongoDB Atlas (cloud)

**Frontend won't start:**
- Delete `node_modules` and `package-lock.json`
- Run `npm install` again

**Port already in use:**
- Kill the process using that port
- Or change port in vite.config.js

---

## 📝 Environment Setup

### Create backend/.env file:
```env
PORT=5000
MONGO_URI=mongodb://localhost:27017/neighborhood-marketplace
JWT_SECRET=your_secret_key_change_this
NODE_ENV=development
```

---

## ✅ What Was Implemented

- ✅ Wishlist count updates in navbar
- ✅ Profile page save functionality  
- ✅ Enhanced filter UI on items page
- ✅ Modern, responsive design
- ✅ Toast notifications
- ✅ Loading states
- ✅ Proper error handling
- ✅ Mobile-friendly layout

---

## 📞 Need Help?

1. Check browser console for errors (F12)
2. Check terminal for server logs
3. Verify MongoDB is running
4. Make sure ports 5000 and 5173 are free
5. Try clearing cache and refreshing

---

**You're all set! Happy testing! 🎉**

For detailed documentation, see: **RUN_COMMANDS.md** and **IMPLEMENTATION_FINAL.md**
